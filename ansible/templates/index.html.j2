<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{{ wife }}!!!</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg1:#FFDEE9;
      --bg2:#B5FFFC;
      --card:#ffffffcc;
      --accent:#ff6b81;
      --accent2:#ffd166;
      --shadow: 0 10px 30px rgba(15,15,30,0.15);
    }
    *{box-sizing:border-box}
    html,body{height:100%;}
    body{
      margin:0;
      font-family: 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
      background: radial-gradient(circle at 10% 10%, rgba(255,255,255,0.18), transparent 10%), linear-gradient(135deg,var(--bg1),var(--bg2));
      display:flex;
      align-items:center;
      justify-content:center;
      padding:32px;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }

    .scene{
      width:100%;
      max-width:980px;
      min-height:420px;
      border-radius:24px;
      background: linear-gradient(180deg, rgba(255,255,255,0.35), rgba(255,255,255,0.12));
      box-shadow: var(--shadow);
      padding:48px;
      position:relative;
      overflow:hidden;
      backdrop-filter: blur(6px) saturate(1.05);
    }

    /* floating soft shapes */
    .blob{
      position:absolute;
      filter: blur(36px) saturate(1.2);
      opacity:0.85;
      transform: translate3d(0,0,0);
      mix-blend-mode: screen;
    }
    .blob.b1{width:360px;height:360px;background:linear-gradient(45deg,var(--accent),#ff9bb3);left:-10%;top:-15%;border-radius:50%;}
    .blob.b2{width:300px;height:300px;background:linear-gradient(45deg,var(--accent2),#b6fffa);right:-8%;bottom:-18%;border-radius:50%;}

    .card{
      display:flex;
      gap:32px;
      align-items:center;
      position:relative;
      z-index:2;
    }

    .text{
      flex:1 1 60%;
    }

    h1{
      font-family:'Playfair Display', serif;
      font-size: clamp(32px, 6vw, 64px);
      margin:0 0 8px 0;
      line-height:1.02;
      letter-spacing: -0.02em;
      color: #2b2b2b;
      text-shadow: 0 6px 18px rgba(255,107,129,0.08);
    }

    .subtitle{
      font-size:clamp(14px,2.2vw,18px);
      margin:0 0 20px 0;
      color:#3b3b3b;
      opacity:0.95;
    }

    .heart{
      display:inline-grid;
      place-items:center;
      background:linear-gradient(180deg,var(--accent),#ff9bb3);
      color:white;
      padding:10px 18px;
      border-radius:999px;
      box-shadow:0 6px 18px rgba(255,107,129,0.18);
      font-weight:600;
      font-size:16px;
      transform-origin:center;
      animation: pulse 1.6s ease-in-out infinite;
    }

    @keyframes pulse{
      0%{transform:scale(1)}
      50%{transform:scale(1.06)}
      100%{transform:scale(1)}
    }

    .portrait{
      width:180px;
      min-width:140px;
      border-radius:20px;
      padding:10px;
      background:linear-gradient(180deg, rgba(255,255,255,0.6), rgba(255,255,255,0.15));
      box-shadow: 0 12px 30px rgba(10,10,20,0.08);
      display:flex;
      align-items:center;
      justify-content:center;
      flex-direction:column;
      gap:10px;
    }

    .portrait .avatar{
      width:110px;height:110px;border-radius:50%;display:block;background:linear-gradient(135deg,#fff4ef,#ffe6f0);display:inline-flex;align-items:center;justify-content:center;font-family:'Playfair Display';font-size:28px;color:#6b2a3d;box-shadow:0 8px 20px rgba(10,10,20,0.06);
    }

    .controls{display:flex;gap:12px;margin-top:12px;flex-wrap:wrap}
    button.action{
      border:0;padding:10px 14px;border-radius:12px;background:linear-gradient(90deg,#ffd166,#ff6b81);color:#2b2b2b;font-weight:600;cursor:pointer;box-shadow:0 8px 20px rgba(255,107,129,0.12);
    }

    /* confetti pieces */
    .confetti-piece{
      position:absolute;opacity:0;pointer-events:none;transform: translate3d(0,0,0) rotate(0deg);
    }

    /* footer */
    .footer{position:absolute;bottom:18px;left:18px;right:18px;font-size:12px;color:#333;opacity:0.7;display:flex;justify-content:space-between;align-items:center}

    /* small screens */
    @media (max-width:720px){
      .card{flex-direction:column;align-items:center;text-align:center}
      .portrait{order:2}
    }
  </style>
</head>
<body>
  <main class="scene" role="main" aria-labelledby="hero-title">
    <div class="blob b1" aria-hidden="true"></div>
    <div class="blob b2" aria-hidden="true"></div>

    <div class="card">
      <div class="text">
        <h1 id="hero-title">Лучшая на свете жена!!!</h1>
        <p class="subtitle">Немного магии, много любви — специально для неё. Нажмите «Праздновать», чтобы засчитать ещё один счастливый день.</p>

        <div class="controls">
          <button class="action" id="celebrateBtn" aria-label="Праздновать">Праздновать 🎉</button>
          <button class="action" id="toggleTheme" aria-label="Сменить тему">Сменить тему ✨</button>
        </div>
      </div>

      <aside class="portrait" aria-hidden="false">
        <div class="avatar" aria-hidden="true">♥</div>
        <div style="font-size:13px;color:#4b4b4b">Для моей любимой</div>
      </aside>
    </div>

    <div class="footer">
      <div>Сделано с любовью 💌</div>
      <div style="font-style:italic">Можете сохранить как HTML и открыть в браузере</div>
    </div>

    <!-- container for confetti pieces -->
    <div id="confetti-container" aria-hidden="true"></div>
  </main>

  <script>
    const celebrateBtn = document.getElementById('celebrateBtn');
    const confettiContainer = document.getElementById('confetti-container');
    const toggleTheme = document.getElementById('toggleTheme');

    function random(min,max){return Math.random()*(max-min)+min}

    function makeConfetti(){
      const colors = ['#ff6b81','#ffd166','#6ee7b7','#8ec5ff','#ff9bb3'];
      const count = 80;
      for(let i=0;i<count;i++){
        const el = document.createElement('div');
        el.className='confetti-piece';
        const size = Math.floor(random(6,14));
        el.style.width = size+'px';
        el.style.height = (size*0.6)+'px';
        el.style.left = random(10,90)+'%';
        el.style.top = random(-20,10)+'%';
        el.style.background = colors[Math.floor(random(0,colors.length))];
        el.style.borderRadius = (random(10,40))+'%';
        el.style.transform = `translate3d(0,0,0) rotate(${random(0,360)}deg)`;
        confettiContainer.appendChild(el);

        // animate
        const fallDuration = random(1400,3200);
        el.animate([
          {transform: `translate3d(0,0,0) rotate(${random(0,360)}deg)`, opacity:1},
          {transform: `translate3d(${random(-80,80)}px, ${random(300,900)}px, 0) rotate(${random(720,1080)}deg)`, opacity:0.9}
        ],{duration:fallDuration, easing:'cubic-bezier(.17,.67,.23,1)', iterations:1});

        // fade out after
        setTimeout(()=>{el.style.opacity='0';}, fallDuration-300);
        // remove
        setTimeout(()=>{el.remove();}, fallDuration+800);
      }
    }

    celebrateBtn.addEventListener('click', ()=>{
      makeConfetti();
      // small heart pop
      const heart = document.querySelector('.heart');
      if(heart){
        heart.animate([{transform:'scale(1)'},{transform:'scale(1.25)'},{transform:'scale(1)'}],{duration:650,easing:'ease-out'});
      }
    });

    // theme toggle (simple)
    toggleTheme.addEventListener('click', ()=>{
      const root = document.documentElement;
      if(root.style.getPropertyValue('--bg1') === ''){
        // set dark-ish theme
        root.style.setProperty('--bg1','#1f1c2c');
        root.style.setProperty('--bg2','#3a3a9a');
        root.style.setProperty('--accent','#ff9bb3');
        root.style.setProperty('--accent2','#ffd166');
      } else {
        // reset to default by clearing inline styles
        root.style.removeProperty('--bg1');
        root.style.removeProperty('--bg2');
        root.style.removeProperty('--accent');
        root.style.removeProperty('--accent2');
      }
    });

    // small accessibility: pressing Enter/Space on focused button triggers
    [celebrateBtn,toggleTheme].forEach(btn=>{
      btn.addEventListener('keyup', (e)=>{ if(e.key === 'Enter' || e.key === ' ') btn.click(); });
    });
  </script>
</body>
</html>

